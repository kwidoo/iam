Possible user registration and login:

with phone
with email

with password
without password (code for phone, email link for email)

UserProfile can use MySql database or MongoDB (todo check other NoSQL databases) DB connection should be defined in config/database.php
